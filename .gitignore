# Secrets
.env

# TLS certs/keys
certs/

# Velociraptor runtime data (generated by the container: server config,
# client configs, datastore, logs, and repacked client binaries)
velociraptor/

# Repacked agent binaries (embed the server's config and CA, never commit)
clients/
*.exe
*.msi
